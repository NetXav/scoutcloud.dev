apiVersion: v1
kind: Service
metadata:
  name: scoutcloud-app
  namespace: scoutcloud
spec:
  type: LoadBalancer
  selector:
    app: scoutcloud
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
